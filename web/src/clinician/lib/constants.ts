/** Shared constants safe to import from both server and client modules. */

/** httpOnly session cookie holding the core-api bearer token after clinician login. */
export const SESSION_COOKIE = "hcos_clinician_session";
